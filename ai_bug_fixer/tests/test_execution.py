def test_run_in_sandbox_returns_exit_code():
    # TODO: run a trivial command ("echo hello") through docker_service
    # and assert exit_code == 0 and "hello" in logs.
    assert True


def test_run_in_sandbox_enforces_timeout():
    # TODO: run "sleep 999" with a short timeout and assert timed_out is True.
    assert True
